<h2><%= @cocktail.name %></h2>
<% @cocktail.dose.each do |dose|%>
  <p><%= dose.description %></p>
<% end %>

<%= link_to "Add a new dose", new_cocktail_dose_path(@cocktail), class: "btn btn-gradient" %>
<%= link_to "Back to cocktails list", cocktails_path, class: "btn btn-gradient" %>
